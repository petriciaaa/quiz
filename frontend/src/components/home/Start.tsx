import { Button } from "@mui/material";
import React from "react";

function Start() {
  return (
    <div className="wrapper w-full h-1/2 flex items-center justify-center">
      <Button variant="contained" color="primary" size="large">
        quiz!
      </Button>
    </div>
  );
}

export default Start;
